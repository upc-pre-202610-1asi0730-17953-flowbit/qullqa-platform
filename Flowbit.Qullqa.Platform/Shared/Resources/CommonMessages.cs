namespace Flowbit.Qullqa.Platform.Shared.Resources;

/// <summary>
///     Marker type for <c>IStringLocalizer&lt;CommonMessages&gt;</c>, resolving to
///     Resources/CommonMessages.resx (default) and CommonMessages.es.resx —
///     generic messages reused across bounded contexts (e.g. unhandled error
///     text), as opposed to each context's own domain-specific *Messages.resx.
/// </summary>
public class CommonMessages
{
}
